{
  "installCommand": "corepack enable && pnpm install",
  "buildCommand": "pnpm --filter @pos/api build:vercel && pnpm --filter @pos/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "functions": {
    "apps/api/api/index.ts": {
      "maxDuration": 30,
      "memory": 512
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "apps/api/api/index.ts"
    }
  ]
}
